apiVersion: v1
kind: Service
metadata:
  name: app-manb-ms
  labels:
    app: app-manb-ms
    backstage.io/kubernetes-id: app-manb-ms
spec:
  selector:
    app: app-manb-ms
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
